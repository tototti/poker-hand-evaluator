# ポーカー役判定プログラム レビューレポート

- **対象**: `pocker1/`（単一ファイル実装） / `pocker2/`（パッケージ分割＋テスト実装）
- **共通仕様書**: `SPEC.md`（両ディレクトリの内容は完全一致することを diff で確認済み）
- **レビュー日**: 2026-09-09

---

## 1. 役判定ロジックの正確性 — 両者とも不具合なし

全 52C5 = **2,598,960 通りの手札**について、pocker1・pocker2・独立に書いた第3の参照実装の3者を突き合わせました。

| 検証項目 | 結果 |
|---|---|
| 役の分類ミスマッチ | **0 件** |
| 役の出現頻度 | ロイヤル 4 / SF 36 / フォーカード 624 / フルハウス 3,744 / フラッシュ 5,108 / ストレート 10,200 / スリーカード 54,912 / ツーペア 123,552 / ワンペア 1,098,240 / ハイカード 1,302,540 — **教科書値に完全一致** |
| タイブレーカー比較（ランダム 30 万ペア） | **不一致 0 件** |
| 同一役内での比較（各役 5 万ペア） | **不一致 0 件** |

A-2-3-4-5 のホイール（最高位を 5 扱い）、A-2-3-4-5 同スート＝ストレートフラッシュ（ロイヤルではない）、ロイヤル同士の引き分けなど、仕様の特殊ケースも両方正しく処理しています。

> **結論: 中核ロジックにバグはありません。**

---

## 2. 見つかった不具合

### pocker2

#### ① `pytest` が単体で起動できない（重要度: 中）

```
ModuleNotFoundError: No module named 'poker'
ModuleNotFoundError: No module named 'python_poker'
```

`python3 -m pytest`（cwd が `sys.path` に入る）でのみ 58 件が通り、素の `pytest` はコレクション段階で 4 ファイルとも落ちます。CI に `pytest` と書いた瞬間に壊れます。

**修正**: `pocker2/pytest.ini` に 1 行追加

```ini
[pytest]
testpaths = tests
pythonpath = .
```

#### ② 仕様書 8 章からの逸脱（重要度: 小）

仕様は「引数が 2 つでない → `ValueError` を送出」ですが、`pocker2/python_poker.py:19-21` は例外を投げずに usage を出して `return 1`。

CLI の観測挙動（stderr ＋ 終了ステータス 1）は満たすものの、他のエラーが `Error: ...` 形式なのにここだけ素の usage で、メッセージ形式も不統一です。

#### ③ `EvaluatedHand` の比較が壊れやすい（重要度: 小）

`pocker2/poker/evaluator.py:48`

- `__eq__` に `isinstance` ガードがなく、`result == "x"` が `AttributeError` になる
- `__eq__` を定義したことで `__hash__` が自動的に `None` になり、**結果オブジェクトを set / dict のキーにできない**（実測で確認済み）

**修正案**:

```python
def __eq__(self, other):
    if not isinstance(other, EvaluatedHand):
        return NotImplemented
    return self._key() == other._key()

def __hash__(self):
    return hash(self._key())
```

#### ④ `Hand.__init__(cards, raw_texts)` の引数名が実態と違う（重要度: 小）

`pocker2/poker/hand.py:7`

- `from_string` が渡しているのは正規化済みの `display_texts` であって raw ではない
- 公開コンストラクタは検証を一切通らず、`Hand([], ["as","ks"])` が作れてしまい、小文字がそのまま出力される

### pocker1

#### ⑤ テストが 1 件も存在しない（重要度: 中）

仕様の要求ではありませんが、pocker2 が 58 件持つのに対しゼロ。

加えて `pocker1/python_poker.py:120` の `main()` は例外処理が `if __name__` ブロック側にあるため、**関数として呼んでもエラー時に終了ステータスを返せず**、テストを書きにくい構造になっています。

#### ⑥ 真偽値判定が脆い（重要度: 小）

`pocker1/python_poker.py:76`, `:94`

```python
if straight_high and is_flush:
```

「`straight_high` が 0 にならない」ことへの暗黙依存。現状は最小値が 5 なので動きますが、`is not None` が正解です（pocker2 はこちらを採用）。

### 共通

#### ⑦ ロイヤルフラッシュのタイブレーカー（重要度: なし）

pocker1 は `()`、pocker2 は `(14,)` としており、仕様の「なし（常に同値）」と厳密には異なりますが、どちらも常に同値になるため実害はありません。

---

## 3. 採点（各 10 点、合計 70 点）

| 観点 | pocker1 | pocker2 | 理由 |
|---|---:|---:|---|
| 役判定の正確性 | 10 | 10 | 全網羅検証で両者とも完全一致 |
| 仕様適合度 | 10 | 8 | pocker2 は引数数エラーで `ValueError` を投げない（②） |
| 可読性 | 7 | 8 | 後述 |
| 拡張性・設計 | 5 | 9 | pocker1 は単一ファイル・役番号が裸の整数。pocker2 は Card / Hand / 評価器を分離、`IntEnum` で意図が明示 |
| テスト | 1 | 8 | pocker1 は皆無。pocker2 は 58 件と厚いが起動が壊れている（①） |
| エラー処理 | 8 | 7 | pocker1 はメッセージが具体的（`Invalid rank: '1S'` vs pocker2 `'1'`）だが `main` が再利用不可。pocker2 は形式不統一 |
| 保守・運用 | 6 | 8 | pocker2 は git / pytest.ini / .gitignore を整備。ただし pytest.ini が機能不全 |
| **合計** | **47 / 70** | **58 / 70** | |

### 可読性についての補足（点差が小さい理由）

pocker2 は `HandRank.FULL_HOUSE` のような命名で明らかに読みやすい一方、**タイブレーカーの作り方が暗黙的**です。

`pocker2/poker/evaluator.py:84-102` はどの役も一律 `tuple(group_values)` を返しており、これは

> 「`(-枚数, -ランク)` でソートした順序が、たまたま仕様書 5 章の比較順と全役で一致する」

という自明でない不変条件に依存しています。正しいのですが、コメントがなく、読み手は自力で検証を強いられます。

対して pocker1 は `four_rank`, `kicker`, `high_pair, low_pair` と毎回明示的に組み立てており、冗長な代わりに仕様書との対応が一目で追えます。

> **「賢さ」では pocker2、「仕様書とのトレーサビリティ」では pocker1** という trade-off。

---

## 4. CLI 挙動の突き合わせ結果

19 パターンの入力で両プログラムを比較。**終了ステータスと判定結果はすべて一致**（差異はエラーメッセージの文面のみ）。

| ケース | pocker1 | pocker2 |
|---|---|---|
| 引数 0 / 1 / 3 個 | 1: usage | 1: usage（`Error:` 接頭辞なし） |
| 小文字入力 | 0: 大文字に正規化 | 0: 同左 |
| 余分な空白 | 0: 正常 | 0: 同左 |
| 末尾カンマ（6 要素） | 1 | 1 |
| 空文字列 | 1 | 1 |
| 4 枚 | 1 | 1 |
| hand 内重複 | 1 | 1 |
| hand 間重複（合法） | 0: Draw | 0: Draw |
| 不正ランク `1S` | 1: `Invalid rank: '1S'` | 1: `Invalid rank: '1'` |
| 不正スート `AX` | 1: `Invalid suit: 'AX'` | 1: `Invalid suit: 'X'` |
| `10S`（3 文字） | 1 | 1 |
| ホイール同スート | 0: Straight Flush | 0: Straight Flush |
| ホイール vs 6 ハイ | 0: Hand 2 | 0: Hand 2 |
| ロイヤル vs ロイヤル | 0: Draw | 0: Draw |
| 全角カンマ | 1 | 1 |

---

## 5. 結論と推奨アクション

- **どちらもポーカーの役判定としては正しく動きます。** バグは周辺（テスト起動・例外設計・API 堅牢性）に集中しています。
- 総合では **pocker2 を推奨**。モジュール分割とテスト資産があり、拡張時のコストが明確に低い。

### 対応の優先順位

1. **① `pytest.ini` に `pythonpath = .` を追加** — 最優先。テストが「動く」と誤認したまま CI に載せるのが最も危険。
2. **③ `EvaluatedHand` の `__eq__` / `__hash__` 修正** — 将来の集合・辞書利用時に顕在化する。
3. **② 引数数エラーを `ValueError` 送出に統一** — 仕様適合とメッセージ形式の一貫性。
4. **④ `Hand.__init__` の引数名修正** — `display_texts` へリネーム、または `from_string` 経由を強制。
